# DECISIONS.md — תובנות עומק והחלטות טכניות

מסמך זה מרכז החלטות והבנות טכניות עמוקות שהתבררו עד כה. עודכן ידנית על ידי בעל הפרויקט (לא Claude Code).

---

## שכבות בטיחות
מעבר לכלל דלתא-T (הפרש PT100/TC מעל 15°C → ניתוק מיידי), מתוכננות עוד שכבות הגנה:
- **Opto1** — אופטו-מבודד שתלוי במצב המשאבה: אם המשאבה לא פועלת, לא מאפשר חימום.
  סטטוס: עקרון מוסכם, טרם מומש בפועל (תלוי בקוד ה-ESP32).
- **Opto2 / הגנת 85°C** — עודכן 2026-09-18: **הוחלט** לממש כיישום חיצוני
  עצמאי לגמרי — בקר טמפרטורה Danfoss שמנתק פיזית את החימום אם הטמפרטורה
  עוברת 85°C, בלי שום תלות ב-ESP32 או בקוד.
  סטטוס: **חדש** — מתוכנן להתבצע חיצונית (חומרה בלבד), כבר לא חוסם את
  קוד ה-ESP32.

## הגנת GFCI/RCD (דגל אדום)
משאבה מס' 2 (230V AC) והאלמנטים עובדים במתח רשת בסביבה רטובה. צריך לבדוק אם קיים מפסק פחת (RCD/GFCI) בלוח שממנו מוזנת המערכת — זו דרישת בטיחות בסיסית, לא תוספת. סטטוס: לא מטופל, דורש בדיקה פיזית של הלוח.

## State Recovery (התאוששות אחרי הפסקת חשמל)
ה-ESP32 שוכח הכל אחרי reset, כולל כמה זמן עבר. פתרון מתוכנן: רכיב RTC מודל DS3231 (שעון עם סוללה עצמאית).
עיקרון: פער קצר → חוזר לפעול לבד; פער ארוך → עוצר ושואל את המשתמש דרך ה-LCD.
פתוח: הסף המדויק בין "קצר" ל"ארוך" עדיין לא נקבע.

## Overshoot בין שלבי Mash
אלמנט ה-RIMS (3500W) עלול לחרוג מעבר לטמפרטורת היעד בגלל אינרציה חומית. צריך לבדוק אם PID רגיל מספיק או שנדרש ramp-rate limiting. סטטוס: שאלה פתוחה.

## LCD כגיבוי
פתוח: האם ה-LCD המקומי רק מציג נתונים, או שאפשר גם לשלוט דרכו (למשל אישור מעבר שלב) אם ה-WiFi נופל.

## התראות מרחוק (רעיון עתידי, לא דחוף)
שליחת התראה לטלפון (למשל ntfy.io או בוט טלגרם) באירועים כמו תחילת רתיחה בפועל או אזהרת בטיחות.

## ארכיטקטורת חיישני טמפרטורה (2026-08-02)
הוחלט להמשיך עם 2×PT100 במקום קסקדה PT100+TC, עקב לוח MAX31855 פגום. יוערך בהמשך אם overshoot בפועל דורש חזרה לטרמוקאפל.

## פערים נוספים שעדיין לא תועדו
- buzzer (זמזם התראה) — חסר
- מסך TFT עתידי — לא הוחלט
- flow switch — נדחה בכוונה לטובת כלל דלתא-T
- פריסת תיבה (enclosure): HV/LV מופרדים פיזית — טיוטה בלבד
- הקצאת פינים ל-ESP32 — טיוטה, לא אושרה סופית
- מידות מדויקות של המשאבות, קוטר צנרת משאבת הקירור — לא תועד

## SSR מחובר ישירות (בלי בידוד גלוואני) — זמני, לבדיקות בלבד (2026-09-03/04)
מודול אופטו-בידוד שנבדק לא הצליח להפעיל את ה-SSR (Fotek SSR-40DA) בשום
תצורה: המתח על כניסת ה-SSR קרס ל-~1.3V תחת עומס אמיתי (במקום 5V מלאים),
גם אחרי החלפת מקור ההזנה וגם אחרי תיקון פולריות (המודול התברר כ-active-low
בצד הפלט). בבדיקה ישירה — ללא המודול — גם ה-SSR וגם ה-GPIO של ה-ESP32
הוכחו תקינים לגמרי. **הוחלט זמנית לחבר את ה-SSR ישירות ל-GPIO, בלי המודול.**

⚠️ **הסתייגות:** במהלך רוב הבדיקות ספק הכוח (באק) שהזין את המודול היה שרוף,
בלי שידענו. לכן אי אפשר לקבוע בוודאות שהמודול עצמו פגום — רק שבתצורות
ובתנאים שנבדקו הוא לא הצליח לספק מספיק זרם. יומן הבדיקות המלא (12 בדיקות
לפי הסדר): `notes/ssr-opto-test-log.md`.

**סיכון שנפתח כתוצאה מזה, וטופל בקוד:** חיבור ישיר מ-GPIO ל-SSR פירושו
שאין שכבת הגנה בין הקוד לחומרה — אם הקושחה נתקעת, ה-SSR יכול להישאר
"תקוע-דלוק" חשמלית בלי שום דבר שיכבה אותו. נוסף watchdog חומרתי (10
שניות) שמאפס את השבב בכפייה אם `loop()` לא מגיב — האיפוס מריץ מחדש את
`setup()`, שמכבה את ה-SSR כצעד הראשון ביותר, לפני כל דבר אחר.

**חובה לפני חיבור 230V אמיתי לפלט של ה-SSR:** להחזיר בידוד גלוואני תקין
(מודול מתאים שנבדק עם ספק כוח תקין ונמדדה צריכת הזרם שלו, או שלב הגברה
עם טרנזיסטור/MOSFET). לא אופציונלי. ראה `ssr-opto-test-log.md` סעיף 6
לרשימת הצעדים הנדרשים.

## שני SSR נפרדים — RIMS ו-Boil, בחירה בקוד לפי שלב (2026-09-18)
יש שני SSR בחומרה: SSR1 (GPIO32) ו-SSR2 (GPIO33) — **אחד לכל אלמנט חימום**
(RIMS / Boil), לא SSR אחד משותף. **הוחלט:** הקוד הוא זה שבוחר איזה SSR
פעיל, לפי שלב הבישול הנוכחי (RIMS בזמן מש, Boil בזמן רתיחה) — **לא בורר
פיזי.** רעיון הבורר הפיזי בוטל.

**פתוח:** המיפוי המדויק — איזה SSR (GPIO32 מול GPIO33) מחובר בפועל לאיזה
אלמנט (RIMS מול Boil) — טרם הוכרע. גם לוגיקת "בחירה לפי שלב" עצמה טרם
מומשה בקוד: נכון לעכשיו `SSR2_PIN` (GPIO33) מוגדר ונכבה כברירת מחדל
ב-`setup()`, ושום דבר בקוד לא מפעיל אותו עדיין (ראה ההערות סביב
`SSR2_PIN` ב-`src/main.cpp`).

## הפרדת SPI מלאה בין שני חיישני PT100 (2026-09-04)
שני לוחות ה-MAX31865 חלקו בעבר קו MOSI משותף. תוצאה: כל הפרעה על הקו הזה
הפילה **את שני** החיישנים בו-זמנית, לא רק את הלוח שנפגע בפועל — מה שיצר
רושם מטעה של קריסת מערכת כוללת, כשבפועל הייתה תקלה אחת שהוכפלה.

**הוחלט:** הפרדה מלאה — לכל לוח SCK/MISO/MOSI/CS משלו, אפס קווים משותפים.
זה אפשרי כי מדובר ב-SPI תוכנתי (bit-bang); הבנאי של `Adafruit_MAX31865`
מקבל את ארבעת הפינים בנפרד לכל אובייקט, ללא תלות בפריפריאל SPI חומרתי
משותף.

**מפת הפינים הנוכחית (`src/main.cpp`):**

| קו | לוח A | לוח B |
|---|---|---|
| SCK | 18 | 16 |
| MISO | 19 | 17 |
| MOSI | 14 | 4 |
| CS | 5 | 22 |

GPIO23 (המוסי המשותף הישן) לא בשימוש. פרטים מלאים: `notes/session-2026-09-04-summary.md` סעיפים 1-2.

## אנקודר EC11 — לא זמין (הוסר 2026-08-28)
**סטטוס:** האנקודר הוסר מהמערכת. במקור אחד (`notes/session-2026-08-28-status.md`,
טבלת פינים) הוא מתועד כ"נשרף"; בהערה ב-`src/main.cpp` הניסוח זהיר יותר —
"appears to be dead" (נראה מת, לא אושר סופית מה בדיוק קרה לו). שני המקורות
מסכימים על התוצאה: **הוא לא פעיל ולא מחובר.**

**הפינים שהיו מוקצים לו:** C (משותף) → GND, CLK → GPIO 4, DT → GPIO 17,
SW → GPIO 16 (הרגל השנייה של המתג → GND).

⚠️ **הפינים האלה כבר לא פנויים.** בעקבות הפרדת ה-SPI המלאה (הסעיף הקודם,
2026-09-04), כל שלושת הפינים נלקחו מחדש עבור לוח PT100 B: `GPIO4`=MOSI,
`GPIO17`=MISO, `GPIO16`=SCK. אנקודר חלופי ידרוש הקצאת פינים אחרים לגמרי,
לא רק "לחבר בחזרה לאותו מקום".

**השלכה על ה-UI:** אין כרגע שום קלט פיזי (סיבוב/לחיצה) על הבקר עצמו.
לפי ההערה ב-`main.cpp`, האנקודר היה אמור להניע בורר תצוגה במסך (סיבוב =
להחליף בין PT1/PT2, לחיצה = להציג את שניהם) — בלעדיו המסך פשוט מציג את
שני החיישנים תמיד, שזה פתרון תפעולי סביר לתצוגה בלבד. אבל **כל אינטראקציה
שדורשת אישור/בחירה פיזית ליד המכשיר עצמו (למשל אישור מעבר שלב אם ה-WiFi
נופל — ראה סעיף "LCD כגיבוי" למעלה) אין לה כרגע שום ערוץ קלט פיזי.** נדרש
פתרון ביניים: אנקודר חלופי על פינים אחרים, כפתור פיזי בודד, או להשאיר את
כל השליטה דרך ה-web UI בלבד (מה שהופך את השאלה ב"LCD כגיבוי" לדחופה יותר).
