# CLAUDE.md — הוראות קבועות ל-Claude Code

## מה זה הפרויקט
YS Smart Brew Controller — בקר בישול בירה DIY, מבוסס ESP32.
תיאור מלא של המערכת הפיזית וה-UI נמצא ב-Project Knowledge בפרויקט Claude נפרד (לא כאן ב-repo).
המשתמש (הבעלים של הפרויקט) הוא מנהל/הוגה רעיונות — לא מבצע טכני יומיומי.
Claude Code הוא זה שכותב, מריץ ומדפיס בפועל את הקוד.

## הוראה קבועה — בסוף כל session
בסיום כל session עבודה, Claude Code חייב:
1. לעדכן את הקובץ `PROGRESS.md` בשורה חדשה: תאריך + מה בוצע.
2. לבצע `git commit` עם הודעה ברורה.
3. לבצע `git push` ל-repo.
אל תסיים session בלי לבצע את שלושת אלה.

## סגנון עבודה מבוקש
- להסביר בעברית, בלי ז'רגון לא מוסבר, כי המשתמש לא מתכנת.
- לפני שינוי משמעותי בקוד — לתאר בקצרה מה הולך להשתנות ולמה.
- קוד ה-ESP32 צריך להיות מודולרי (למשל: PID רגיל היום, קסקדה PT100+TC בעתיד — שדרוג = החלפת פונקציה אחת, לא שכתוב הכל).

## הקשר טכני מהיר
- MCU: ESP32
- חיישנים: PT100 (MAX31865), תרמוקאפל Type-K (MAX31855)
- בטיחות: כלל דלתא-T (הפרש PT100/TC מעל 15°C → ניתוק), בעתיד גם Opto1/Opto2 (ראו DECISIONS.md)
- UI: דפדפן מקומי מול IP של ה-ESP32, WebSocket לתקשורת real-time
- לפרטים מלאים על הציוד הפיזי (כלים, ברזים, משאבות, מסלול זרימה) — לשאול את המשתמש להביא מהפרויקט הנפרד, אל תניח פרטים.

## מה לא לעשות
- אל תמציא פרטי חומרה שלא אושרו (למשל מידות משאבות שלא תועדו).
- אל תשנה את ההחלטה ש-repo זה Public.
